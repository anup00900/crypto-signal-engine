# ===========================================
# CRYPTO DATA COLLECTOR - ENVIRONMENT CONFIG
# ===========================================
# Rename this file to .env and fill in your values

# ----- Database Configuration -----
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=crypto_data

# Admin credentials (for setup and migrations)
POSTGRES_ADMIN_USER=crypto_admin
POSTGRES_ADMIN_PASSWORD=your_secure_admin_password_here

# Collector credentials (for data insertion)
POSTGRES_COLLECTOR_USER=crypto_collector
POSTGRES_COLLECTOR_PASSWORD=your_secure_collector_password_here

# Analyst credentials (read-only access for analysis)
POSTGRES_ANALYST_USER=crypto_analyst
POSTGRES_ANALYST_PASSWORD=your_secure_analyst_password_here

# API credentials (read-only access for external applications)
POSTGRES_API_USER=crypto_api
POSTGRES_API_PASSWORD=your_secure_api_password_here

# ----- Deribit API Configuration -----
# Public endpoints don't require auth, but authenticated access has higher rate limits
DERIBIT_API_URL=https://www.deribit.com/api/v2
DERIBIT_TEST_API_URL=https://test.deribit.com/api/v2

# Optional: For higher rate limits (leave empty for public access)
DERIBIT_CLIENT_ID=
DERIBIT_CLIENT_SECRET=

# Use test network for development (true/false)
DERIBIT_USE_TESTNET=false

# ----- Binance API Configuration -----
BINANCE_API_URL=https://api.binance.com/api/v3
BINANCE_FUTURES_URL=https://fapi.binance.com/fapi/v1

# Optional: For higher rate limits
BINANCE_API_KEY=
BINANCE_API_SECRET=

# ----- CoinGecko API Configuration -----
COINGECKO_API_URL=https://api.coingecko.com/api/v3

# Optional: Pro API key for higher rate limits
COINGECKO_API_KEY=

# ----- Data Collection Settings -----
# Number of years of historical data to collect
HISTORY_YEARS=2

# Delay between API requests (milliseconds)
REQUEST_DELAY_MS=100

# Maximum retries for failed requests
MAX_RETRIES=3

# Batch size for database inserts
DB_BATCH_SIZE=1000

# ----- Logging Configuration -----
LOG_LEVEL=INFO
LOG_FILE=logs/collector.log
LOG_ROTATION=10 MB
LOG_RETENTION=30 days

# ----- Scheduler Settings -----
# Daily update time (UTC)
DAILY_UPDATE_HOUR=1
DAILY_UPDATE_MINUTE=0


